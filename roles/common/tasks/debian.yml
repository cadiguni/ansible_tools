---
- name: Atualizar cache APT
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Instalar pacotes base no Debian/Ubuntu
  ansible.builtin.apt:
    name: "{{ common_packages }}"
    state: present
    install_recommends: true
